{
  "name": "$AGENT_NAME",
  "description": "Agent with env var substitution",
  "instructions": "Do stuff with $AGENT_SECRET.",
  "model": {
    "provider": "anthropic",
    "name": "claude-sonnet-4-20250514"
  },
  "inbound": {
    "type": "resend",
    "webhookSecret": "$WEBHOOK_SECRET",
    "apiKey": "$API_KEY"
  },
  "actions": [
    { "type": "log" }
  ],
  "server": { "port": "$TEST_PORT" }
}
